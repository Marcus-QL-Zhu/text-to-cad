# Generated and extracted CAD/CAE files are ignored instead of stored in LFS.
# Only the original, attributed third-party escapement archive is non-regenerable.
models/watch_kinematic/references/escapement/swiss_lever_grabcad_snapshot_15/swiss-lever-watch-escapement-model-1.snapshot.15.zip filter=lfs diff=lfs merge=lfs -text

# Non-model media may still use LFS where required.
*.mp4 filter=lfs diff=lfs merge=lfs -text
